<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:meatup="http://schemas.android.com/apk/res-auto">
    <!-- Item for contextual menu -->
    <item
            android:id="@+id/action_create_invitation"
            android:icon="@drawable/ic_action_new_email"
            android:title="Create Invitation"
            meatup:showAsAction="ifRoom"
            android:visible="false"
            />
    <!-- Items for normal menu -->
    <item
            android:id="@+id/action_add_friend"
            android:icon="@drawable/ic_action_add_person"
            android:title="Add Friend"
            meatup:showAsAction="ifRoom"
            />
    <item
            android:id="@+id/action_settings"
            android:title="Settings"
            meatup:showAsAction="never"
            />
    <item
            android:id="@+id/action_help"
            android:title="Help"
            meatup:showAsAction="never"
            />
</menu>